/**
 * This is the main route's path for calls
 * @type {{path: string, exact: boolean}}
 */
export const callsRoute = {
  path: "/home",
  exact: true
};

export const registerRoute = {
  path: "/register",
  exact: true
};